'gbp-repos':
  ? 'You must update to a newer rosdep version by calling..sudo apt-get update &&
    sudo apt-get install python-rosdep (make sure to uninstall the pip version on
    Ubuntu)'
  : !!null 'null'
'release-name': 'groovy'
'repositories':
  'actionlib':
    'url': 'https://github.com/ros-gbp/actionlib-release.git'
    'version': '1.9.11-0'
type: gbp
